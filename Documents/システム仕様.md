# ImaginAI - システム仕様

## 1. システム概要

ImaginAIは、AI画像・動画・音楽・音声生成APIを専用GUIで操作するデスクトップアプリケーションである。Electronで構築され、複数のAPIプロバイダを並列にサポートする:

- **Google AI Studio** (内部識別子: `gemini`): Gemini Nano Banana 系 (画像) / Veo 3.1・Gemini Omni Flash (動画) / Lyria 3 (音楽) / Gemini TTS (音声)。`@google/genai` SDK 経由で呼び出す。
- **OpenAI** (内部識別子: `openai`): gpt-image-2 / gpt-image-1.5 / gpt-image-1 (画像)。`openai` SDK 経由で呼び出す。

両プロバイダはデータスキーマ・UI 構造ともに完全に対称な形で扱われる。コード内識別子は `gemini` / `openai` で統一し、ユーザー向け表示文字列のみ「Google AI Studio」「OpenAI」を使う二層構造で運用する。

## 2. アーキテクチャ

### 2.1 プロセスモデル

- **Mainプロセス**: IPC通信、ファイルI/O、API呼び出し、設定管理、APIキー暗号化、ウィンドウ管理を担当する。
- **Preloadプロセス**: `contextBridge`を通じてMainプロセスとRendererプロセスを安全に橋渡しする。
- **Rendererプロセス**: React + MUI によるUIを担当する。

### 2.2 ディレクトリ構造

```
src/
  main/
    index.ts              # アプリエントリ、ウィンドウ生成、コアIPC
    ipc/index.ts          # アプリケーションIPCハンドラ
    services/
      settings-service.ts # 設定の永続化 (JSON)
      api-key-service.ts        # safeStorageによるAPIキー暗号化、起動時マイグレーション、プロバイダ別キー管理
      history-service.ts        # 履歴CRUD、サムネイル、ZIPエクスポート、ロード時マイグレーション
      generation-service.ts     # プロバイダ振り分け dispatcher (provider に基づき gemini/openai-service を呼び分け)
      gemini-service.ts         # @google/genai SDK 経由の Gemini API 通信 (画像・動画・音楽・音声)
      openai-service.ts         # openai SDK 経由の OpenAI API 通信 (画像 generate / edit)
      ffmpeg-service.ts         # ffmpegによる動画サムネイル抽出 + TTS PCM→MP3 エンコード
    utils/
      console-bridge.ts   # DevToolsコンソール転送
  preload/
    index.ts              # セキュアIPCブリッジ
  renderer/
    main.tsx              # アプリエントリポイント
    components/
      TitleBar.tsx         # カスタムタイトルバー (ウィンドウ制御 + プロバイダグループ化された API キー選択)
      MainPage.tsx         # メイン画面レイアウト
      ParameterPanel.tsx   # 生成パラメータ制御 (プロバイダで完全切替、provider 別サブコントロール)
      PromptPanel.tsx      # プロンプト・ネガティブプロンプト・参照画像・画像編集モード切替
      GenerateButton.tsx   # 生成実行ボタン
      HistoryPanel.tsx     # 履歴グリッド (provider/mediaType フィルタ + コンテキストメニュー)
      NoticesDialog.tsx    # 利用上の注意 (両プロバイダ縦並びセクション)
      ProviderIcon.tsx     # プロバイダ用ブランドグリフ (Gemini スパーク / OpenAI スパイラル)
      SettingsPage.tsx     # 設定画面 (ProviderApiKeySection 共通コンポーネントで両プロバイダ対称配置)
    stores/
      app-store.ts         # アプリレベルの状態 (Zustand)
      generation-store.ts  # 生成パラメータの状態
      history-store.ts     # 履歴エントリの状態
    i18n/
      config.ts            # i18next設定
      locales/
        en.ts              # 英語翻訳
        ja.ts              # 日本語翻訳
  shared/
    types.ts              # 共有型定義
    constants.ts          # 定数、モデル定義、IPCチャンネル
    ipc.ts                # IPC API型定義
```

## 3. 機能仕様

### 3.1 画像生成

- ユーザーはモデルを選択し、パラメータ（アスペクト比、解像度、生成枚数）を設定し、プロンプトを入力し、任意で参照画像を添付する。
- パラメータはモデルの対応状況に基づいて検証される（例: 21:9は対応モデルのみ、4Kは対応モデルのみ）。モデルが画像入力非対応の場合は参照画像UIが非表示、ネガティブプロンプト非対応の場合はネガティブプロンプト欄が非表示、生成枚数が1枚のみのモデルでは枚数スライダーが非表示となる。
- 出力フォーマットはPNG固定。セーフティフィルターはAPI側で制御されており、ユーザー選択不可。
- 参照画像はファイル選択、ドラッグ＆ドロップ、または履歴エントリから追加できる。サムネイルとして表示され、個別に削除可能。
- 「生成」ボタンでAPIリクエストを送信する。生成中はボタンが無効化され、ローディングインジケータが表示される。
- 成功時は結果が自動的に履歴に保存される。失敗時のメッセージは2層構造で扱う:
  - **インフラ系エラー**（HTTP 4xx/5xx、gRPCステータス、ネットワーク等）: ユーザー向けに行動指針付きで言い換えたローカライズ文言を表示。
  - **コンテンツ系ソフト失敗**（HTTP 200だが生成物0件: Safetyブロック、Recitation、モデル拒否等）: 「{種別}が生成されませんでした。詳細を確認してください。」のみ事実として表示する（プロンプト変更を促す不確かなアドバイスは出さない）。
- 「詳細」リンクをクリックすると、APIから返された診断情報（`finishReason`、`promptFeedback.blockReason`、`safetyRatings`、テキストパートに含まれる拒否理由、Veoの`raiMediaFilteredReasons`等）を生で確認できる。
- ネットワークエラー発生時はリトライボタンを表示し、ユーザーがリトライ可否を選択できる。
- 生成前に履歴保存先のディスク空き容量を確認し、100MB未満の場合は警告を表示する。
- 履歴件数が設定された上限（10,000件）を超えている場合、生成がブロックされる。

### 3.2 履歴管理

- 各生成結果はメタデータ（JSON）と生成ファイル（画像/動画/音楽）として保存される。
- 履歴はサムネイルグリッドでフレックスレイアウト表示され、更新日降順でソートされる。ページネーション（50件ずつ）によるインフィニティスクロール（遅延ロード）で表示される。
- 左クリックでモードレスビューアウィンドウに画像を表示する（動画の場合は動画ビューアで再生、音楽の場合は音楽プレイヤーで再生）。複数枚生成の場合、すべてをそれぞれ別ウィンドウで開く。
- 右クリックでコンテキストメニューを表示: プロンプトに追加、名前をつけて保存（複数枚の場合は各画像のダイアログを順次表示）、パラメータ復元、削除。
- ヘッダーメニュー: すべて削除、すべて圧縮保存して削除（ZIPエクスポート、圧縮中はプログレスバーを表示）。
- 各エントリには生成所要時間（API呼び出し開始から完了まで）が記録され、履歴カードに表示される。
- 履歴パネルの高さは上辺のドラッグで調整可能。
- プロンプトテキストによる部分一致検索でフィルタリング可能。

### 3.3 動画生成

動画モデルは提供 API が2系統ある。どちらを使うかはモデル定義の `gemini.videoApi` フィールドで宣言する（省略 = Veo の predictLongRunning 経路、`'interactions'` = Interactions API 経路）。

#### Veo 3.1 系

- Veo 3.1モデル（Lite/Fast/Standard）による動画生成に対応する。
- パラメータ: 長さ（4/6/8秒）、動画解像度（720p/1080p/4k）、アスペクト比（16:9/9:16のみ）、ネガティブプロンプト、シード値。
- Veoモデル選択時は画像固有のUI（出力解像度）が非表示となり、動画固有のUI（長さ、動画解像度、シード値）が表示される。
- 動画生成はVeo APIの非同期ポーリング（Long-Running Operation）方式で行われ、生成中は経過時間が表示される。ポーリング間隔: 10秒。
- 生成された動画はMP4形式で履歴ディレクトリに保存される。
- サムネイルはffmpegにより1秒目のフレームから抽出される。ffmpegが利用不可の場合はサムネイルなし。
- 1080p/4K解像度の場合、長さは8秒のみ指定可能（生成ボタンで検証）。
- 参照画像を添付することで画像→動画生成（Image-to-Video）が可能。
- 音声はモデルにより自動的に生成される（ユーザー制御不可）。
- 人物生成の制御はモードに応じてAPIが自動的に適用する（ユーザー制御不可）。

#### Gemini Omni Flash（`gemini-omni-flash-preview`）

- Interactions API（`interactions.create`）経由の動画生成モデル。2026/6/30 パブリックプレビュー公開。
- 出力は **720p / 24fps 固定、長さ 3〜10 秒**。長さ・BGM・効果音を制御する API パラメータは存在せず、**プロンプトで指示**する（モデルセレクタ下に `gemini.model.note.omniFlash` の注釈を表示）。
- パラメータはアスペクト比（16:9 / 9:16）のみ。長さ・動画解像度セレクタは `supportedDurations` / `supportedResolutions` を宣言しないことで自動的に非表示となり、generation-store も対応モデル以外では `duration` / `resolution` をリクエスト・履歴に含めない。
- ネガティブプロンプトは API 非対応（`supportsNegativePrompt` 未宣言のため UI 非表示）。
- 参照画像は最大2枚（公式ドキュメントの例示上限。明示的な上限は未公表）。画像→動画生成（Image-to-Video）に対応。
- リクエストは同期型（LRO ポーリングなし）で、呼び出しが完了まで数分ブロックすることがある。経過時間表示はサービス内のローカルタイマー（5秒間隔）で `GENERATION_PROGRESS` イベントを発火して実現する。
- レスポンスの動画は inline（Base64、`output_video.data`）が既定。大きい動画で URI（`output_video.uri`）が返された場合は `x-goog-api-key` ヘッダー認証でダウンロードする。
- レスポンスメタデータとして interaction ID（`HistoryEntry.gemini.interactionId`。将来 `previous_interaction_id` による会話型編集に利用可能）とトークン使用量（`usageTokens`）を履歴エントリに記録する。
- 会話型動画編集（`previous_interaction_id`）・動画入力（Files API）は現時点では未対応（テキスト/画像→動画のみ）。

### 3.4 音楽生成

- Lyria 3モデル（Clip/Pro）による音楽生成に対応する。
- **Lyria 3 Clip** (`lyria-3-clip-preview`): 30秒の短いクリップを生成。$0.04/曲。
- **Lyria 3 Pro** (`lyria-3-pro-preview`): 最大約3分のフルレングス楽曲を生成。$0.08/曲。曲の長さはプロンプトで指定。
- Lyriaモデル選択時は画像・動画固有のUI（アスペクト比、解像度、生成枚数、長さ、シード値）がすべて非表示となる。
- プロンプトでジャンル、雰囲気、楽器、BPM、キー、歌詞（`[Verse]`, `[Chorus]`, `[Bridge]`セクションタグ）、タイムスタンプ指定が可能。歌詞に関する指示をプロンプトに含めることで、歌詞付き楽曲を生成できる。
- 参照画像を添付することで画像→音楽生成（Image-to-Music）が可能。Lyria の公式上限は 10 枚だが、本アプリでは `ModelDefinition.maxReferenceImages` で **5 枚に制限**している。

#### 参照画像の前処理（image / video / music 共通）

全ての生成経路で、APIに送る前に Electron `nativeImage` で以下の前処理を行う:

1. デコード
2. 長辺が **1920px** を超える場合のみ、縦横比を維持して縮小（`quality: 'best'`）
3. **JPEG (品質 85) に再エンコード**（PNG/WebP 入力も JPEG に統一）

枚数上限は各モデル定義の `maxReferenceImages` フィールドで宣言する（省略または 0 は画像入力非対応）。現状の値:

| モデル | maxReferenceImages |
| --- | --- |
| Nano Banana 系 (gemini-2.5-flash-image / gemini-3.1-flash-image / gemini-3-pro-image) | 10 |
| Nano Banana 2 Lite (gemini-3.1-flash-lite-image) | 14 |
| Veo 3.1 系 | 1 |
| Gemini Omni Flash (gemini-omni-flash-preview) | 2 |
| Lyria 3 系 | 5 |
| Gemini TTS 系 | 未定義（画像入力非対応） |

動画 (Veo) は枚数 1 の単一スロットモデルとして扱い、UI で新しい画像を添付すると **直前の画像を上書き**する。参照画像エリアには「開始フレーム」ラベルを表示してユーザに用途を明示する。Gemini Omni Flash は最大2枚の通常マルチスロットとして扱う。

- ネガティブプロンプトは非対応。
- 生成された音楽は履歴ディレクトリに保存される。保存形式（拡張子）はAPIが返した`mimeType`に基づいて決まる（通常はMP3、48kHz ステレオ、SynthIDウォーターマーク付き）。
- APIレスポンスにテキストパート（歌詞、楽曲説明、BPM等のメタデータ）が含まれる場合、`audioTexts`フィールドにparts順序のまま配列として保存される。テキストパートの内容や数はAPIの挙動に依存し、構造的なラベル区分はない。
- 履歴パネルではサムネイルの代わりに音楽アイコンで表示される。
- クリックで音楽プレイヤーウィンドウ（モードレス）が開き、HTML5 `<audio>` 要素による再生コントロール付きで自動再生される。テキストパートがある場合はプレイヤーウィンドウ内にパートごとに区切り線付きで表示される。
- APIは`/v1beta/models/{model}:generateContent`エンドポイントを使用し、`responseModalities: ['AUDIO', 'TEXT']`を指定する。レスポンスのpartsの順序は保証されないため、すべてのpartsをイテレートして音声データ（`inlineData` with `audio/*`）とテキストデータを分離する。
- Single-turn only（マルチターン非対応）。

### 3.4.1 音声合成（TTS）

- Gemini TTSモデル（Flash TTS Preview / Pro Preview TTS / Flash Preview TTS）によるテキスト読み上げ生成に対応する。
- **Gemini 3.1 Flash TTS Preview** (`gemini-3.1-flash-tts-preview`): 最新モデル。Audio Tags対応。Preview 段階。料金は Input $1.00 / 1M tok、Audio Output $20.00 / 1M tok。
- **Gemini 2.5 Pro Preview TTS** (`gemini-2.5-pro-preview-tts`): 品質重視。Preview 段階。料金は Input $1.00 / 1M tok、Audio Output $20.00 / 1M tok。
- **Gemini 2.5 Flash Preview TTS** (`gemini-2.5-flash-preview-tts`): コスパ重視。Preview 段階。料金は Input $0.50 / 1M tok、Audio Output $10.00 / 1M tok。
- 無料枠の RPM / TPM / RPD は 2025/12 以降 Google が公開固定値を廃止し、プロジェクト単位で AI Studio の Rate Limits ダッシュボードに表示される方式に変更されている。本アプリでは無料枠キー使用時に、共通の参考値（3 RPM / 10K TPM、1分あたり最大3リクエスト、合計で約6分程度の音声生成）を表示する。実際の上限は各ユーザーの AI Studio プロジェクト設定に従う。
- TTSモデル選択時、ParameterPanelに以下を追加表示する:
  1. **スタイル**: 9種のプリセット + `カスタム`の選択肢を持つドロップダウン（表示形式 "スタイル名 : 効果"）。プリセット選択で下段のスタイル内容欄に該当する英語プロンプトが自動展開される。
  2. **スタイル内容**: 複数行テキスト入力。手動で編集するとスタイル選択は自動的に`カスタム`になる。
  3. **ボイス**: 30種の事前定義ボイスを "ボイス名 : 特徴" 形式で選択。
- プロンプト欄のラベルは「話させたい内容」に差し替わる。参照画像UIは非表示。
- Gemini 3.1 Flash TTS Preview のみ、プロンプト欄ラベル右に `?` アイコンを表示。クリックで Audio Tags 詳細解説ダイアログ（Expressions / Prosody / Pause セクション）を開く。
- APIリクエスト:
  - エンドポイント: `/v1beta/models/{model}:generateContent`
  - `generationConfig.responseModalities: ['AUDIO']`
  - `generationConfig.speechConfig.voiceConfig.prebuiltVoiceConfig.voiceName` に選択したボイス名を指定
  - テキスト本文は `Style: <英語スタイル指示>, Text: <ユーザー入力>` の形式で合成（スタイル指示が空なら本文のみ）
- APIは raw PCM（例: `audio/L16;codec=pcm;rate=24000`）で返却される。後処理は次の順で行う:
  1. メモリ上で同梱ffmpegを介して MP3 に変換（CBR 160 kbps, 48 kHz, mono）。成功時は `.mp3` で保存。
  2. ffmpegが利用不可・失敗した場合は、純JSで RIFF/WAV ヘッダ（44バイト）を付与した WAV を `.wav` で保存。これによりAPIが成功している（=トークンが消費されている）にもかかわらず後処理失敗で結果が破棄されることを防ぐ。
- `HistoryEntry.styleInstruction` / `voice` にも保存され、履歴からの復元で元のスタイル/ボイスに戻る。
- 1リクエストあたりの最大テキスト長は 4,000 バイト（API制限）。

### 3.5 画像ビューア

- モードレスウィンドウとして開く（複数同時に開くことが可能）。
- 初期サイズは画像の等倍ピクセルサイズ。画面サイズの90%を超える場合は縮小して表示する。
- ウィンドウリサイズ時に画像は縦横比を維持して拡大・縮小する。

### 3.6 動画ビューア

- モードレスウィンドウとして開く。HTML5 `<video>` 要素による再生コントロール（再生/一時停止、シーク、音量）付き。
- 自動再生で開始される。

### 3.7 設定

- **言語**: i18nextによる日本語/英語切り替え。変更は即時反映。翻訳データは`src/renderer/i18n/locales/`に一元管理される。
- **テーマ**: ライト/ダーク/システム（OS追従）。変更は即時反映。
- **APIキー**: Electronの`safeStorage`で暗号化保存。マスク表示と表示/非表示トグル付き。以下の3種類のキーを最大7つ（デフォルト1・無料枠1・カスタム最大5）まで保持可能。
  - **デフォルトキー**: タイトルなし。通常利用想定。
  - **無料枠キー**: タイトルなし。このキーを選択中はモデルの料金ではなく無料利用枠の情報が表示される。
  - **カスタムキー**: タイトル付きで最大5つ追加可能。各キーに「無料枠キーとして扱う」フラグを設定できる。
  - キャプションバー（タイトルバー）の切替UIで、現在有効なキーを切り替える。アクティブキーが生成APIリクエストに使用される。
  - アクティブキーが無料枠種別の場合、ParameterPanelではモデル毎の参考価格は非表示となり、代わりに無料利用枠の情報（利用可否等）が表示される。両者は相反関係で同時表示されない。
- **履歴保存先**: ユーザーが任意のディレクトリを選択可能。変更時に既存履歴の移動有無を確認するダイアログを表示。

### 3.8 配布アーティファクト後処理 (Windows portable の zip 化)

- `electron-builder.yml` の `afterAllArtifactBuild` フックに `scripts/zip-portable.js` を指定し、Windows portable `.exe` をビルド完了後に `.zip` 圧縮する。未署名 `.exe` を直接配布するとブラウザやウイルス対策ソフトの警告が厳しいため、portable 版は zip 形式で配布する。
- フック内の処理:
  1. `context.platformToTargets` を走査し、Windows プラットフォーム上に `portable` ターゲットが含まれる場合のみ処理を行う (macOS / Linux ビルドでは何もせず通過する)。
  2. `context.artifactPaths` から `.exe` を抽出し、同 basename の `.exe.blockmap` が `artifactPaths` に含まれない `.exe` のみを portable 版として扱う。NSIS は `.exe` と `.exe.blockmap` を必ずペアで出力するため、`.blockmap` を伴わない `.exe` は確実に portable 版である。
  3. `archiver` (zlib level 9) で同名 `.zip` を生成し、成功後に元 `.exe` を削除する。
  4. 生成した zip パスの配列を戻り値として返す。electron-builder はこれを publish 対象アーティファクトに追加するため、`yarn release:win` 実行時には GitHub draft release に zip が含まれる。
- NSIS 関連アーティファクト (`.exe`, `.blockmap`, `latest.yml` など) は変更・削除しない。

## 4. データフロー

### 4.1 生成フロー

1. Renderer: ユーザーがパラメータを設定し、生成ボタンをクリック
2. Renderer -> Main (IPC): `generation:execute`でGenerationParamsを送信
3. Main: 履歴件数が上限を超えていないか検証
4. Main: API呼び出し
   - 画像: Gemini generateContent
   - 動画 (Veo): Veo predictLongRunning → LROポーリング → 動画ダウンロード
   - 動画 (Omni): Interactions API `interactions.create`（同期）→ inline Base64 または URI ダウンロード
   - 音楽: Lyria generateContent → 音声・テキストパート抽出
5. Main: 生成ファイルとメタデータを履歴ディレクトリに保存
6. Main: サムネイルを生成（画像: Electron nativeImage、動画: ffmpeg、音楽: サムネイルなし）
7. Main -> Renderer (IPC): HistoryEntryを返却
8. Renderer: 履歴一覧を再読み込み

### 4.2 API通信

- **Nano Bananaモデル**（例: gemini-3.1-flash-image）: `/v1beta/models/{model}:generateContent`エンドポイントを使用し、`responseModalities: ['TEXT', 'IMAGE']`を指定。ネガティブプロンプトはテキストプロンプト内に埋め込み。1枚ずつ生成。参照画像入力対応。
- **Veo 3.1モデル**（例: veo-3.1-generate-preview）: `/v1beta/models/{model}:predictLongRunning`エンドポイントを使用。レスポンスはLong-Running Operation（LRO）として返され、`/v1beta/{operationName}`を10秒間隔でポーリングする。完了時に動画のダウンロードURLを取得し、`x-goog-api-key`ヘッダーで認証してダウンロードする。ネイティブ音声生成を含む。ポーリングはタイムアウトなしで完了まで待機する（公式SDK準拠）。
- **Gemini Omni Flashモデル**（gemini-omni-flash-preview）: Interactions API（`/v1beta/interactions`、SDK の `ai.interactions.create`）を使用。入力はコンテンツブロック配列（`{type: 'image', data, mime_type}` の参照画像 + `{type: 'text', text}` のプロンプト）、出力形式は `response_format: {type: 'video', aspect_ratio: '16:9' | '9:16'}` で指定する。リクエストは同期型で、レスポンスの `output_video` から動画（Base64 または URI）を取得する。`status !== 'completed'` または動画欠落時は `NO_VIDEO_GENERATED` エラーとして `status` / `output_text` を診断情報に載せる。
- **Lyria 3モデル**（例: lyria-3-pro-preview）: `/v1beta/models/{model}:generateContent`エンドポイントを使用し、`responseModalities: ['AUDIO', 'TEXT']`を指定。レスポンスのpartsにはaudio（inlineData, audio/mpeg）とtext（歌詞・楽曲説明・メタデータ等）が混在し、順序不定のためイテレートして分離する。テキストpartsは構造的なラベル区分がないため、配列としてそのまま保存する。参照画像入力対応（Image-to-Music）。
- 参照画像はリクエスト時にローカルファイルパスからBase64エンコードして送信
- APIキーはリクエスト時に暗号化ストレージから取得

## 5. セキュリティ

- APIキーはElectronの`safeStorage`により暗号化して保存される。
- RendererプロセスはAPIキーに直接アクセスせず、すべてのAPI操作はMainプロセス経由のIPC通信で処理される。
- コンテキスト分離とPreloadスクリプトにより、WebコンテンツとNode.js APIの間に安全な境界を確保する。

## 6. 拡張性

- モデル定義は`shared/constants.ts`に一元管理されている。新モデル追加時はこのファイルのみを更新する。
- 各モデル定義には対応パラメータが含まれ、UIの有効/無効制御が自動的に行われる。生成バックエンドのディスパッチは`mediaType`で行い、画像はすべて Gemini generateContent 経路で処理される。動画は`gemini.videoApi`フィールドでさらに Veo（predictLongRunning、省略時）と Interactions API（`'interactions'`、Gemini Omni Flash）に分岐する。ネガティブプロンプトのAPI対応可否（`apiNegativePrompt`）もモデル定義で管理される。
- モデル定義のAPIプロバイダフィールドにより、将来的な複数プロバイダ対応に備えている。
- APIキーストレージはプロバイダ別にキー管理されており、複数APIキーに対応可能。

### 6.1 廃止モデルの扱い

提供元がシャットダウンしたモデルは、生成用の `MODEL_DEFINITIONS` から取り除き、`RETIRED_MODEL_DEFINITIONS` へ移動する。これにより新規生成では一切選択・実行できなくなる（生成モデル選択UI・プロバイダディスパッチは `MODEL_DEFINITIONS` のみを参照する）。一方で、過去にそのモデルで生成した履歴は引き続き完全に利用可能とする:

- 履歴フィルターのモデル一覧は `ALL_MODEL_DEFINITIONS`（= `MODEL_DEFINITIONS` + `RETIRED_MODEL_DEFINITIONS`）をマージし、実際に履歴に存在するモデルIDに絞り込んで構築する。廃止モデルの履歴が存在する場合のみフィルター候補に現れる。
- 履歴エントリのモデル名表示は保存済みの `modelDisplayName` を用いるため、定義の有無に依存しない。
- 廃止モデルの履歴からパラメータを復元する際は、`RETIRED_MODEL_DEFINITIONS[].replacementModelId`（移行先モデルID）に置換したうえで、移行先モデルの対応パラメータ（アスペクト比・解像度・枚数・参照画像数など）にクランプする。これによりプロンプト・フィルター等は引き継ぎつつ、モデルとしては必ず移行先が選択される。
- 現在の廃止モデル:
  - Imagen 4 ファミリ（`imagen-4.0-fast-generate-001` / `imagen-4.0-generate-001` / `imagen-4.0-ultra-generate-001`）。いずれも 2026/6/24 にシャットダウン。移行先は Nano Banana 2（`gemini-3.1-flash-image`）。
  - Nano Banana 2 / Pro の preview 版（`gemini-3.1-flash-image-preview` / `gemini-3-pro-image-preview`）。GA 版（`gemini-3.1-flash-image` / `gemini-3-pro-image`）のリリース（2026/5/28）に伴い、preview 版は 2026/6/25 にシャットダウン。移行先はそれぞれの GA 版。

## 7. 定義定数

| 定数名 | 値 | 説明 |
|---|---|---|
| HISTORY_MAX_COUNT | 10000 | 生成がブロックされる履歴上限件数 |
| HISTORY_PAGE_SIZE | 50 | インフィニティスクロールの1ページあたりの読み込み件数 |
| GENERATION_COUNT_MIN | 1 | 1回の生成における最小枚数 |
| GENERATION_COUNT_MAX | 4 | 1回の生成における最大枚数 |
| DEFAULT_ASPECT_RATIO | "16:9" | デフォルトのアスペクト比 |
| DEFAULT_QUALITY | "1k" | デフォルトの出力解像度 |
| DEFAULT_NUMBER_OF_IMAGES | 1 | デフォルトの生成枚数 |
| DEFAULT_MODEL_ID | "gemini-3.1-flash-image" | デフォルトのモデル（Nano Banana 2） |
| DEFAULT_DURATION | 4 | デフォルトの動画の長さ（秒） |
| DEFAULT_RESOLUTION | "720p" | デフォルトの動画解像度 |
| COST_REFERENCE_DATE | "2026.7.14" | コスト情報の参照日（公式料金/Deprecations を 2026/7/14 時点で再確認） |
| API_KEY_CUSTOM_MAX | 5 | カスタムAPIキーの最大登録数 |
| THUMBNAIL_SIZE | 300 | サムネイルの長辺最大サイズ（ピクセル） |

## 8. モデル料金一覧 / サポート終了日

Google AI Studio (Gemini API) の最新情報を `COST_REFERENCE_DATE` 時点で再確認した結果。料金は `MODEL_DEFINITIONS[].costLabel` に同期されている。提供元が終了日を公表しているモデルには `MODEL_DEFINITIONS[].shutdownDate`（例: `'2026/10/2'`）を設定する。設定されている場合、(1) ドロップダウンの各項目には日付なしの警告色チップ「終了予定」を表示し、(2) モデルセレクタ直下には日付入りの注釈行（`サポート終了: {日付}`）を表示する。`noteKey` は Lyria のクリップ長などモデル終了とは無関係の機能注釈にのみ用いる（終了日とは別管理）。

| メディア | モデルID | 表示名 | 参考料金 | サポート終了日 |
| --- | --- | --- | --- | --- |
| 画像 | `gemini-2.5-flash-image` | Nano Banana | $0.039/image | **2026/10/2** |
| 画像 | `gemini-3.1-flash-lite-image` | Nano Banana 2 Lite | 1K: $0.034/image（1K のみ対応。画像編集モード非対応・2K/4K 非対応・参照画像は最大14枚） | 未発表（GA・2026/6/30） |
| 画像 | `gemini-3.1-flash-image` | Nano Banana 2 | 512px: $0.045 / 1K: $0.067 / 2K: $0.101 / 4K: $0.151（/image 概算） | 未発表（GA） |
| 画像 | `gemini-3-pro-image` | Nano Banana Pro | 1K/2K: $0.134 / 4K: $0.24（/image 概算） | 未発表（GA） |
| 動画 | `veo-3.1-lite-generate-preview` | Veo 3.1 Lite | 720p: $0.05/sec / 1080p: $0.08/sec | 未発表（Preview） |
| 動画 | `veo-3.1-fast-generate-preview` | Veo 3.1 Fast | 720p: $0.10 / 1080p: $0.12 / 4K: $0.30（/sec） | 未発表（Preview） |
| 動画 | `veo-3.1-generate-preview` | Veo 3.1 | 720p/1080p: $0.40/sec / 4K: $0.60/sec | 未発表（Preview） |
| 動画 | `gemini-omni-flash-preview` | Gemini Omni Flash | 720p: 約$0.10/sec（正式には出力動画トークン課金: $17.50/1M tok × 5,792 tok/sec） | 未発表（Preview・2026/6/30 公開） |
| 音楽 | `lyria-3-clip-preview` | Lyria 3 Clip | $0.04/song | 未発表（Preview） |
| 音楽 | `lyria-3-pro-preview` | Lyria 3 Pro | $0.08/song | 未発表（Preview） |
| TTS | `gemini-2.5-flash-preview-tts` | Gemini 2.5 Flash TTS | Input $0.50 / Output $10.00（/1M tok） | 未発表（Preview） |
| TTS | `gemini-2.5-pro-preview-tts` | Gemini 2.5 Pro TTS | Input $1.00 / Output $20.00（/1M tok） | 未発表（Preview） |
| TTS | `gemini-3.1-flash-tts-preview` | Gemini 3.1 Flash TTS | Input $1.00 / Output $20.00（/1M tok） | 未発表（Preview） |

備考:

- Nano Banana 2 / Nano Banana Pro の正式な課金単位は image tokens（output 1M トークン単位、$60 / $120 ）+ text tokens（input $0.50 / $2.00、output $3 / $12）の組合せだが、UI上は分かりやすさを優先して1枚あたり概算を表示する。
- Preview ステータスのモデルは料金が予告なく変更される可能性がある。
- OpenAI の `gpt-image-1` / `gpt-image-1.5` は 2026/12/1 に API から削除予定（公式廃止予告）。生成は引き続き可能だが、モデル選択UIにサポート終了日（`openai.model.note.gptImageShutdown`）を表示する。後継は `gpt-image-2`。なお `gpt-image-2` は現行最新で廃止予定はない。

### 8.1 廃止モデル（生成不可）

シャットダウン済みで `RETIRED_MODEL_DEFINITIONS` に移動したモデル。新規生成では選択できないが、過去の履歴からの閲覧・フィルター・パラメータ復元は可能（復元時は移行先モデルに置換される）。

| メディア | モデルID | 表示名 | 旧参考料金 | シャットダウン日 | 移行先 |
| --- | --- | --- | --- | --- | --- |
| 画像 | `imagen-4.0-fast-generate-001` | Imagen 4 Fast | $0.02/image | 2026/6/24 | Nano Banana 2 (`gemini-3.1-flash-image`) |
| 画像 | `imagen-4.0-generate-001` | Imagen 4 | $0.04/image | 2026/6/24 | Nano Banana 2 (`gemini-3.1-flash-image`) |
| 画像 | `imagen-4.0-ultra-generate-001` | Imagen 4 Ultra | $0.06/image | 2026/6/24 | Nano Banana 2 (`gemini-3.1-flash-image`) |
| 画像 | `gemini-3.1-flash-image-preview` | Nano Banana 2 (Preview) | 512px〜4K: $0.045〜$0.151/image | 2026/6/25 | Nano Banana 2 GA (`gemini-3.1-flash-image`) |
| 画像 | `gemini-3-pro-image-preview` | Nano Banana Pro (Preview) | 1K/2K: $0.134 / 4K: $0.24 /image | 2026/6/25 | Nano Banana Pro GA (`gemini-3-pro-image`) |
