<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Hardened Runtime Permissions -->
    <!-- Required for Electron to function under Hardened Runtime (Notarization) -->
    <!-- Note: App is not sandboxed, so sandbox-related entitlements are not needed -->

    <!-- Allow JIT compilation (required by V8 JS engine) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required by Electron) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (required to load third-party dylibs/plugins) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow DYLD environment variables (required by Electron for native module loading) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Allow user-selected file access (file dialogs for image save/load) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow reading/writing to user home directory -->
    <!-- Required for accessing:
         - ~/.imaginai (app config)
         - User-specified history save directories
    -->
    <key>com.apple.security.temporary-exception.files.absolute-path.read-write</key>
    <array>
      <string>/Users/</string>
    </array>

    <!-- Allow outgoing network connections (required for AI generation API calls) -->
    <key>com.apple.security.network.client</key>
    <true/>
  </dict>
</plist>
